# CLAUDE.md

이 저장소에서 작업할 때 Claude Code가 따라야 하는 지침입니다.

## 작업 원칙

1. **모든 설명은 한국어로 답하기** — 커밋 메시지 등 저장소에 남는 산출물이 아닌, 사용자에게 보이는 모든 설명/응답은 한국어로 작성한다.
2. **모바일(안드로이드) 최적화 우선** — 사용자는 모바일 환경에서만 사용하므로, 이 저장소의 모든 프로젝트/코드는 안드로이드 환경 기준으로 최적화해서 만든다.
3. **코딩 전에 먼저 생각하기 (Think First)** — 코드를 작성하기 전에 구조와 접근 방식을 먼저 정리하고, 애매하면 진행 전에 확인한다.
4. **단순하게 먼저 (Keep It Simple)** — 처음부터 과도한 추상화나 미래를 위한 확장성을 넣지 않는다. 필요한 만큼만 구현한다.
5. **외과적으로 정밀하게 수정하기 (Surgical Modifications)** — 요청받은 범위를 벗어나는 리팩터링이나 부수적인 변경을 하지 않는다. 변경은 최소 범위로, 목적에 정확히 맞게 한다.
6. **목표 중심으로 실행하기 (Task-Oriented)** — 매 작업은 사용자가 요청한 목표 달성에 집중하고, 곁가지 작업으로 흐르지 않는다.

## 프로젝트 구성

- `index.html` — 기존 다이어트 트래커 웹 페이지 (독립적, 이 문서의 안드로이드 지침과 무관).
- `raw-editor/` — 안드로이드 네이티브 RAW 이미지 편집 앱 (Kotlin, Jetpack Compose, LibRaw/NDK 기반). 신규 기능은 원칙적으로 이 폴더 하위에서 진행한다.
